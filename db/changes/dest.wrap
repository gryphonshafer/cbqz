#!/usr/bin/env perl
use exact;
use Config::App;

my ( $dbname, $username, $password ) = @{ Config::App->new->get('database') }{ qw( dbname username password ) };

system( qq{/usr/bin/env cat $ARGV[0] | /usr/bin/env mysql --skip-column-names -u$username -p'$password' $dbname} );
